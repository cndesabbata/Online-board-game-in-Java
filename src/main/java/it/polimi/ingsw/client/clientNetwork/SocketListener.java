/* package it.polimi.ingsw.client.clientNetwork;

import it.polimi.ingsw.client.view.ClientView;
import it.polimi.ingsw.client.view.MessageHandler;

import java.io.ObjectInputStream;
import java.net.Socket;

public class SocketListener implements Runnable{

    private final Socket socket;
    private final ClientView view;
    private final MessageHandler messageHandler;
    private final ObjectInputStream inputStream;

    public SocketListener(Socket socket, ClientView view, MessageHandler messageHandler, ObjectInputStream inputStream) {
        this.socket = socket;
        this.view = view;
        this.messageHandler = messageHandler;
        this.inputStream = inputStream;
    }

    @Override
    public void run() {

    }
} */
